#  CheckerBoard


class Checker_board(width = 3, height = 3, invert = FALSE)
  def initialize
    @width = width
    @height = height
    @invert = invert
    # possibly add attr reader and change methods later

    # add invert conditional to toggle
    toggle = 0
    h = Array.new(@height)
    w = Array.new(@width)
    for i in 1..h.length
      if toggle == 0
        for ii in 1..w.length

        end
        toggle = 1
      elsif toggle = 1
        for ii in
        end
      else
        #exception
      end
    end
  end

end


